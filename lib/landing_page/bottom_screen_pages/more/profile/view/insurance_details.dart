import 'package:flutter/material.dart';

class InsuranceDetails extends StatelessWidget {
  const InsuranceDetails({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Insurance"),),
    );
  }
}
