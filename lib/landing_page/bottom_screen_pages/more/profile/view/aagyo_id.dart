import 'package:flutter/material.dart';

class AagyoId extends StatelessWidget {
  const AagyoId({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("ID"),),
    );
  }
}
